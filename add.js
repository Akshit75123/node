// vid - 9 and 10
// our first module

const addTwoNumbers = (a, b) => {
  return a + b;
};

module.exports = addTwoNumbers;
//default export
//exports is an object which helps to store the value of require function in some variable.
// the value of this object is what the rquire module return to the corresponding module.
